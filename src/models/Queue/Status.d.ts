export declare type QueueStatuses = {
    success: string
    waiting: string
    inProgress: string
    failed: string
}

export const queueStatuses:QueueStatuses = {
    success: 'success',
    waiting: 'waiting',
    inProgress: 'inProgress',
    failed: 'failed',
}